{
  "buildCommand": "pip install -r requirements.txt && python model/train_model.py",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.py": {
      "memory": 3008,
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/app.py"
    }
  ]
}
